{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if grep -qE '\"file_path\": ?\"[^\"]*(index\\.html|/docs/[^\"]*\\.html|\\.css)\"'; then echo 'Edited a GitHub Pages file - apply the update.pages skill: ship it, watch the pages build for the pushed SHA, and report live/stuck/failed proactively.' >&2; fi; true"
          }
        ]
      }
    ]
  }
}
