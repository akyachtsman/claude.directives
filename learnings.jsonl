{"ts": "2026-07-21T19:05:00Z", "type": "preference", "key": "logical-map-artifact-mirror-retired", "text": "Owner decision 2026-07-21: the logical-map Artifact mirror (claude.ai/code/artifact/f20dda86-f0ac-4dba-b2a1-a191e6c3b034) is RETIRED — never republish it after map changes. Fully redundant with the permanent public Pages URL, and each republish costs an unavoidable web permission prompt (claude.ai web sessions apply no permission allowlists). The map's only maintained home is docs/site/logical-map.html served via Pages.", "confidence": 9, "files": ["docs/site/logical-map.html"], "slug": null}
{"ts": "2026-07-21T19:35:00Z", "type": "architecture", "key": "physical-map-retired", "text": "Owner decision 2026-07-21: docs/site/repo-map.html (physical-folders view) is deleted; docs/site/logical-map.html is the repo's single map. The add-a-file inventory drill now touches ONE map (SELF frame counts + Σ), CLAUDE.md layout table, and this file. Legacy stub docs/repo-map.html redirects to the logical map.", "confidence": 9, "files": ["docs/site/logical-map.html", "docs/repo-map.html"], "slug": null}
{"ts": "2026-08-16T09:10:00Z", "type": "tool", "key": "github-rest-vs-graphql-quota", "text": "GitHub REST and GraphQL are separate quota pools drawn by every session on the account. Merging is REST; leaving draft is GraphQL-only, so a GraphQL-exhausted session can merge a non-draft PR but not un-draft one. Un-draft as soon as CI is green. Diagnose from the failing verb, and treat a throttle as retryable, never as a broken repo.", "confidence": 9, "files": ["directives/git.md", "MAINTAIN-REPO-USER-INSTRUCTIONS.md"], "slug": null}
{"ts": "2026-08-16T09:10:00Z", "type": "pattern", "key": "assert-outcome-not-mechanism", "text": "A UI suite written from the implementation confirms the implementation. Name the observable outcome first, then assert against it: 'the legend opens', not 'the handler fires'. Proven 2026-08-01 on this repo's map, where a 27-assertion Playwright gate stayed green through four interaction bugs a human found in minutes.", "confidence": 9, "files": ["directives/test.md", ".github/scripts/check-repo-map-ui.js"], "slug": null}
{"ts": "2026-08-16T09:10:00Z", "type": "architecture", "key": "pr-draft-default-reconsidered", "text": "REJECTED: changing the PR-draft default to open ready-for-review. Raised after an exhausted GraphQL pool stalled three green PRs at un-draft. The cause was session volume, not the draft policy, and git.md's existing mitigation already covers it — un-draft as soon as CI goes green. Opening ready-for-review stays a per-PR judgement when already quota-strained.", "confidence": 8, "files": ["directives/git.md", "directives/global.md"], "slug": null}
{"ts": "2026-08-16T09:10:00Z", "type": "tool", "key": "pr-subscribe-is-harness-side", "text": "Creating a PR subscribes the session to its activity harness-side; no tool call is involved and omitting one prevents nothing. subscribe_pr_activity exists to take over a PR the session did not open, or to re-subscribe after unsubscribing. The only lever on your own PR is unsubscribe_pr_activity after the fact.", "confidence": 9, "files": ["directives/git.md"], "slug": null}
{"ts": "2026-08-16T09:10:00Z", "type": "pitfall", "key": "codex-label-race-at-merge", "text": "Never judge Codex from the codex-flagged label: written asynchronously, so absence proves nothing though presence still blocks. Match reviews and clean comments to HEAD by SHA and verify Codex authored them; a bare 👍 has no SHA, so accept it only when its review request postdates the push. Read inline comments; if that GraphQL call fails, the gate is uncleared.", "confidence": 9, "files": ["directives/git.md", ".github/workflows/codex-monitor.yml", "plugins/directives-toolkit/agents/pr-readiness-reviewer.md"], "slug": null}
{"ts": "2026-08-16T09:10:00Z", "type": "architecture", "key": "skipped-test-gate-discriminator", "text": "A skipped-test CI gate cannot key on whether a skip carries a reason string: every shipped test.skip has one, including the fixture-absence skips it must catch. Key an allowlist by stable test identity — project, spec file, full test title — checked against a JSON reporter run, so intended skips are enumerated once and any new skip fails.", "confidence": 6, "files": ["templates/actions/ui-suite/action.yml", "templates/ui-tests/tests/app.spec.js", "directives/test.md"], "slug": null}
{"ts": "2026-08-19T04:00:00Z", "type": "pitfall", "key": "installed-tooling-needs-repo-hook", "text": "One shared environment serves every repo, and its cached image keeps whatever plugins the installer once installed. Re-saving the Setup script does not reliably rebuild it. So an installer fix reaches only repos that commit .claude/hooks/session-start.sh. Symptom: one repo spews a plugin error while claude.directives is clean — the difference is the hook, not the environment.", "confidence": 9, "files": ["scripts/install-toolkit.sh", "templates/claude-hooks/session-start.sh", "MAINTAIN-REPO-USER-INSTRUCTIONS.md"], "slug": null}
{"ts": "2026-08-21T21:47:00Z", "type": "pitfall", "key": "assert-every-scripted-edit-applied", "text": "A Python str.replace() whose needle does not match returns the string unchanged and raises nothing. A scripted fix silently no-oped and the commit message then claimed a change the file never received. Assert every scripted edit applied (assert s.count(a)==1 before replacing), and verify claims against the file as written.", "confidence": 10, "files": []}
{"ts": "2026-08-22T02:30:00Z", "type": "pattern", "key": "detection-exhaustive-values-allowlisted", "text": "A validator has two surfaces needing OPPOSITE treatment: accepted VALUES allowlisted, DETECTION exhaustive and fail-closed. Narrowing detection silently skips input — how check-landing-cards.js broke. But exhaustiveness has a cost ceiling: there the owner ruled six residual gaps acceptable (2026-08-22) rather than add a parser dependency. Aim for it; never reopen a ruled limit.", "confidence": 9, "files": [".github/scripts/check-landing-cards.js"]}
{"ts": "2026-08-22T17:27:53Z", "type": "pitfall", "key": "ruleset-bypass-exempts-actor", "text": "A GitHub ruleset bypass entry exempts an ACTOR, not a workflow or a token. Bypassing the account that owns a PAT exempts every push that account makes — sessions included — while the ruleset still reads Active. Recall whenever a scheduled workflow's direct push collides with branch protection: delete the workflow, never widen the bypass.", "confidence": 9, "files": ["MAINTAIN-REPO-USER-INSTRUCTIONS.md", "templates/workflows/keepalive.yml"], "slug": null}
{"ts": "2026-08-22T17:27:53Z", "type": "pitfall", "key": "reconcile-consumers-not-just-statements", "text": "Changing a rule in one file leaves every file that ACTS on it stating the opposite. Re-confirmed three times inside one PR (#266): the exported directive, then the bootstrap scaffold, then four docs still demanding a now-unused secret. After any rule change grep case-INSENSITIVELY for every name involved, and fix the consumers in the same commit.", "confidence": 10, "files": [], "slug": null}
{"ts": "2026-08-22T21:29:26Z", "type": "pitfall", "key": "shallow-clone-answers-history-confidently", "text": "Session clones are SHALLOW by default, so git log answers history questions from a truncated window with no error — a 16-day view returned a confident \"active\" to a 60-day threshold downstream. Before any claim from log depth (age, gaps, last-touched, authorship): git rev-parse --is-shallow-repository, then git fetch --unshallow. Bare --deepen exits 129, and --deepen N can still land short of the cutoff.", "confidence": 9, "files": [], "slug": null}
{"ts":"2026-08-22T22:30:00Z","type":"pitfall","key":"concurrent-sessions-same-pr","text":"Two sessions can drive one PR at once. Before acting on a PR you did not open, check it is not still moving: compare updated_at and newest commit time against now, and read existing comments. A flagged label with unresolved threads is often a session mid-iteration, not an abandoned PR; duplicating its Codex re-request spends the account-wide weekly pool.","confidence":9,"files":[],"slug":null}
{"ts": "2026-08-22T23:13:30Z", "type": "pitfall", "key": "unratified-rules-read-as-consistent", "text": "An unratified company-wide rule is self-consistent, so a consistency audit never flags it. The provenance pass has TWO failure modes: it fires on a section that merely defers to a stamped ruling, and it can be talked out of a real finding by an entailment argued from a ruling that does not actually reach that far (design.md's token mechanism, caught by Codex 2026-08-22). Deference must be cited, never constructed.", "confidence": 8, "files": ["plugins/directives-toolkit/commands/audit-repo.md", "directives/data.md", "directives/design.md"]}
{"ts": "2026-08-22T23:40:00Z", "type": "tool", "key": "ruleset-probe-via-api-not-push", "text": "Probe a default-branch protection rule through the GitHub contents API (create_or_update_file, branch=main), never git push: a remote session's push scope covers only its designated branch, so a push to main returns a sandbox 403 indistinguishable from the rule working. Use a fresh probe filename each attempt.", "confidence": 9, "files": ["MAINTAIN-REPO-USER-INSTRUCTIONS.md", "directives/git.md"]}
{"ts": "2026-08-23T08:55:00Z", "type": "tool", "key": "codex-review-triggers", "text": "A plain push does NOT re-trigger Codex. Its only triggers are opening a PR for review, marking a draft ready, and an explicit '@codex review' comment. Silence after a push means never asked, not unavailable — do not take git.md's documented-unavailable path on a head that was only pushed. Measured 2026-08-23: an explicit request drew a review in 4-7 minutes every time.", "confidence": 9, "files": ["directives/git.md"], "slug": null}
{"ts": "2026-08-23T08:55:01Z", "type": "pitfall", "key": "codex-flagged-can-describe-a-stale-head", "text": "codex-monitor clears codex-flagged for the SHA it reviewed and cannot un-clear on a later push, so an absent label may describe a superseded head. The label is weaker evidence than git.md's gate, which wants a Codex response naming the CURRENT head. Check commit_id, not the label — #283 would otherwise have merged its most substantive change unreviewed.", "confidence": 9, "files": [".github/workflows/codex-monitor.yml"], "slug": null}
{"ts": "2026-08-23T08:55:02Z", "type": "pitfall", "key": "rule-filed-where-its-actor-cannot-read-it", "text": "Three times in one session a rule landed in a file its actor never opens: an owner-wide delivery rule in internal learnings, a session instruction in a runbook /refresh-repo never reads, and a fleet ruling in referenced docs while the inherited directives went untouched. Before writing a rule, name who executes it and confirm they read that file.", "confidence": 9, "files": ["directives/git.md", "MAINTAIN-REPO-USER-INSTRUCTIONS.md"], "slug": null}
{"ts": "2026-08-23T08:55:03Z", "type": "pitfall", "key": "static-playwright-config-read-fails-silently", "text": "Proving viewport coverage by reading playwright.config.js as text failed across three implementations and eleven findings — quoting, spread order, // inside URLs, testMatch, screen.width, hard-coded UI_TESTS_DIR. Every failure was SILENT. `--list --reporter=json` cannot help: it reports viewport null. Import the config and devices table so Node expands the spread.", "confidence": 9, "files": ["templates/ui-tests/playwright.config.js"], "slug": null}
{"ts": "2026-08-23T08:55:04Z", "type": "pitfall", "key": "sweep-the-tree-not-a-remembered-file-list", "text": "Changing a rule, I grepped an explicit file list chosen from memory. Three were right; the file I did not think of was the one that mattered, and would have reversed the ruling through its own documentation. Grep the whole tree for the old wording before claiming a rule change is complete.", "confidence": 8, "files": [], "slug": null}
{"ts": "2026-08-23T21:10:00Z", "type": "pitfall", "key": "codex-label-race-at-merge", "text": "Supersedes the 2026-08-16 entry, whose 👍 exception is WITHDRAWN. Never judge Codex from the codex-flagged label: written asynchronously, so absence proves nothing though presence still blocks. Match reviews and clean comments to HEAD by SHA and verify Codex authored them; read inline comments, and if that GraphQL call fails the gate is uncleared. A bare 👍 NEVER clears the gate: the reactions payload carries counts only — no author and no timestamp — so it cannot be attributed to Codex and cannot be correlated with a review request. 'Accept it when the request postdates the push' has nothing to compare against and would merge an unreviewed head. A clean verdict leaves no review at all, only that reaction, readable via issue_read get (pull_request_read get has no reactions field) — so an empty review list means 'has not run' OR 'ran clean', and a reaction-only clean round cannot clear codex-flagged, since the monitor fires on reviews and comments but never reactions.", "confidence": 9, "files": ["directives/git.md", "docs/standards/automations.md", ".github/workflows/codex-monitor.yml", "plugins/directives-toolkit/agents/pr-readiness-reviewer.md"], "slug": null}
{"ts": "2026-08-23T21:11:00Z", "type": "pitfall", "key": "sweep-the-tree-not-a-remembered-file-list", "text": "Supersedes the earlier entry, whose remedy — 'grep the whole tree for the old wording' — is what failed. Withdrawing the 👍 merge exception in directives/git.md, I swept and still shipped: docs/standards/automations.md and learnings.jsonl carried the same rule in different words, and plugins/.../pr-readiness-reviewer.md reached the right verdict from a falsifiable reason. The second copy of a claim never uses the first copy's words. Sweep for the CLAIM: name the fact being withdrawn (here 'a reaction can date itself'), enumerate every consumer that ACTS on it — directives, exported docs, agents, hooks, and learnings.jsonl, which is itself a consumer — and check each against the fact, not against a string. A consumer that agrees with the new rule for a reason the new rule disproves is still a defect: the reason is what the next reader will reuse.", "confidence": 9, "files": ["directives/git.md", "docs/standards/automations.md", "MAINTAIN-REPO-USER-INSTRUCTIONS.md", "plugins/directives-toolkit/agents/pr-readiness-reviewer.md", "learnings.jsonl"], "slug": null}
{"ts": "2026-08-23T21:35:00Z", "type": "pitfall", "key": "codex-label-race-at-merge", "text": "Supersedes the 21:10 entry, which overstated the reaction limitation. Never judge Codex from the codex-flagged label: written asynchronously, so absence proves nothing though presence still blocks. Match reviews and clean comments to HEAD by SHA and verify Codex authored them. On reactions, be precise about WHICH VIEW: the embedded summary in issue_read->get carries counts only (no author, no timestamp), so a 👍 read there cannot clear the gate and 'accept it when the request postdates the push' has nothing to compare against. The data is missing from that view, NOT from GitHub — GET /repos/{owner}/{repo}/issues/{number}/reactions returns user and created_at. So a clean round IS reachable, and the test is an ORDERING: push -> review request -> Codex-authored reaction, each strictly after the last. 'Created after the push' alone is insufficient, because a review of the PREVIOUS head can still be in flight when a new commit lands and its reaction then postdates that push while describing the old SHA. Where the list endpoint is unreachable (it was from a Claude Code remote session on 2026-08-23: direct REST 'GitHub access is not enabled for this session', WebFetch 403), ask Codex a direct question naming the SHA, or attest on the PR. What is forbidden is clearing the gate silently. A clean verdict leaves no review at all, and a reaction-only clean round cannot clear codex-flagged — the monitor fires on reviews and comments, never reactions.", "confidence": 9, "files": ["directives/git.md", "docs/standards/automations.md", ".github/workflows/codex-monitor.yml", "plugins/directives-toolkit/agents/pr-readiness-reviewer.md"], "slug": null}
{"ts": "2026-08-23T21:36:00Z", "type": "pitfall", "key": "sweep-the-tree-not-a-remembered-file-list", "text": "Supersedes the 21:11 entry — which prescribed the right discipline and was then itself missed by it, twice in one hour. Sweep for the CLAIM, not the wording: name the fact being withdrawn, enumerate every consumer that ACTS on it, and check each against the fact. Two additions learned the hard way. (1) learnings.jsonl is a consumer of every claim it records, INCLUDING the claim that you must sweep consumers; when a fact changes, the entry stating it is the first thing to supersede, not an afterthought — it is read at session start, so a stale one propagates further than any doc. (2) A twinned pair (directives/git.md and directives/global.md here) fails this way repeatedly: a rule corrected in one is left standing in the other, four separate times across one PR. Treat any known twin as a single edit site. A consumer that agrees with the new rule for a reason the new rule disproves is still a defect — the reason is what the next reader reuses, and mine was falsified one review round later.", "confidence": 9, "files": ["directives/git.md", "directives/global.md", "docs/standards/automations.md", "MAINTAIN-REPO-USER-INSTRUCTIONS.md", "plugins/directives-toolkit/agents/pr-readiness-reviewer.md", "learnings.jsonl"], "slug": null}
{"ts": "2026-08-23T22:05:00Z", "type": "pitfall", "key": "reaction-ordering-is-not-attribution", "text": "Timestamps establish ORDER; order is not attribution. A Codex 👍 carries no SHA, so 'created after the push' and even the full push -> review request -> reaction ordering are NECESSARY but NOT SUFFICIENT: a request made on the OLD head and still in flight when you push produces a reaction that satisfies every timestamp comparison while describing the old commit. The reaction-list rung clears only when every earlier request already has a SHA-bearing response on record — and an unanswered earlier request is exactly what a previous clean round looks like, so on a PR that has already run clean once that rung is usually unavailable. Compounding it: a GitHub user holds at most one reaction of a given type per subject, so a SECOND clean verdict adds no new reaction at all — the only 👍 present is the first, with its original timestamp (read from the documented reaction model, not measured here). Practical consequence: ask Codex a direct question naming the SHA (answered as an authored, timestamped comment) or attest on the PR. The general lesson beyond Codex: when a signal lacks an identifier, no amount of timestamp comparison recovers one — correlate by identity or stop claiming correlation.", "confidence": 8, "files": ["directives/git.md", "docs/standards/automations.md", "plugins/directives-toolkit/agents/pr-readiness-reviewer.md"], "slug": null}
