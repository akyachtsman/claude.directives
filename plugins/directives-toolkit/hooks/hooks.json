{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "in=$(cat); if echo \"$in\" | grep -qE '\"file_path\": ?\"[^\"]*(index\\.html|/docs/[^\"]*\\.html|\\.css)\"' && ! echo \"$in\" | grep -qE '\"file_path\": ?\"[^\"]*/templates/'; then echo 'Edited a GitHub Pages file - apply the update-pages skill: ship it, watch the pages build for the pushed SHA, and report live/stuck/failed proactively.' >&2; exit 2; fi; exit 0"
          },
          {
            "type": "command",
            "command": "in=$(cat); msg=''; if echo \"$in\" | grep -qE '\"file_path\": ?\"[^\"]*CLAUDE\\.md\"'; then msg='CLAUDE.md changed - the copy injected at session start is now stale; re-read the file before relying on project instructions.'; elif echo \"$in\" | grep -qE '\"file_path\": ?\"[^\"]*(\\.claude/settings\\.json|plugins/directives-toolkit/(agents|hooks|scripts)/)'; then msg='Hook/agent configuration changed - it loads only at session start; takes effect next session, do not assume it is live.'; fi; if [ -n \"$msg\" ]; then echo \"$msg\" >&2; exit 2; fi; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/push-gate.sh"
          },
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/wait-gate.sh"
          }
        ]
      }
    ]
  }
}
