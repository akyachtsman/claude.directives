/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel auto-detects Next.js; no special config needed for the default
  // production tier. Add image domains, redirects, etc. here as the app grows.
};

export default nextConfig;
