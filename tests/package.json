{
  "name": "claude-directives-theme-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Optional Playwright test for the design-system color-scheme contract",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
